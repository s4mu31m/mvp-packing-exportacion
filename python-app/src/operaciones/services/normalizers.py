def normalize_code(value: str) -> str:
    return (value or "").strip().upper()


def normalize_temporada(value: str) -> str:
    return (value or "").strip()


def normalize_operator_code(value: str) -> str:
    return (value or "").strip().upper()
