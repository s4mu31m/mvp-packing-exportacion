# 03.4 — Mapeo a Dataverse
 
> **Subpágina de [[03 Modelo de datos y entidades]].** Este documento especifica el tipo Dataverse de cada atributo del modelo objetivo. **Dataverse es la base estructural principal de datos del MVP**, solicitado por el cliente para mantener consistencia con sus otros módulos. Este documento representa la estructura objetivo principal del MVP en datos. Las diferencias entre lo especificado aquí y lo implementado actualmente en Django (ver [[03.1 · Modelo Implementado en Django]]) son diferencias de avance de implementación, no de criterio. Complementa [[03.2 ‐ Especificación del modelo]] y [[03.3 ‐ Diagrama entidad ‐ relación (ERD)]].
 
---
 
> Este documento especifica el tipo Dataverse de cada atributo del modelo. Complementa [03.2 - Especificación del modelo](03.2-‐-Especificación-del-modelo).
 
---
 
## Convenciones de tipos
 
| Tipo Dataverse | Cuándo se usa en este modelo |
|---|---|
| **Single line of text** | Códigos, nombres, textos cortos, horas en formato `HH:mm` |
| **Multiline text** | Observaciones, notas y descripciones que pueden ser largas |
| **Whole number** | Contadores, cantidades, segundos — sin decimales |
| **Decimal number** | Kilos, temperaturas, porcentajes, pH — precisión exacta |
| **Date only** | Fechas operacionales sin hora |
| **Date and time** | Timestamps de auditoría |
| **Two options** | Campos sí / no |
| **Choice** | Listas cerradas de valores definidos |
| **Lookup** | Relación (FK) a otra tabla de Dataverse |
 
> **Nota sobre horas:** Dataverse no tiene tipo `TIME` nativo. Todos los campos de hora se almacenan como `Single line of text` en formato `HH:mm`.
 
> **Nota sobre auditoría:** Dataverse provee columnas nativas `Created On` y `Modified On` que se llenan automáticamente. Los campos `created_at` y `updated_at` se implementan con esas columnas nativas — no es necesario crearlas manualmente.
 
> **Nota sobre `rol`:** presente en todas las tablas de proceso como `Single line of text`. El backend determinará los 5 perfiles permitidos y su lógica de validación.
 
---
 
## Índice de tablas
 
- [Bin](#bin)
- [LotePlanta](#loteplanta)
- [BinLotePlanta](#binloteplanta)
- [CamaraMantencion](#camaramantencion)
- [Desverdizado](#desverdizado)
- [CalidadDesverdizado](#calidaddesverdizado)
- [IngresoAPacking](#ingresoapacking)
- [RegistroPacking](#registropacking)
- [ControlProcesoPacking](#controlprocesopacking)
- [Pallet](#pallet)
- [PalletLotePlanta](#palletloteplanta)
- [CalidadPallet](#calidadpallet)
- [CamaraFrio](#camarafrio)
- [MedicionTemperaturaSalida](#mediciontemperaturasalida)
 
---
 
## Bin
 
Unidad atómica del proceso. Código interno: `YYYYMMDD-0001` (`id_bin`). Código de barras externo: `bin_code`.
 
**Restricciones de negocio:** `id_bin` es inmutable una vez emitido. `kilos_neto_ingreso` no puede superar `kilos_bruto_ingreso`.
 
| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `id_bin` | Single line of text | PK interna generada por el sistema. Inmutable. |
| `bin_code` | Single line of text | Código de barras del bin — externo o formado en recepción |
| `contador_incremental` | Whole number | Resetea por día |
| `fecha_cosecha` | Date only | Base para `id_bin` |
| `codigo_productor` | Single line of text | |
| `nombre_productor` | Single line of text | |
| `codigo_sag_csg` | Single line of text | |
| `codigo_sag_csp` | Single line of text | |
| `codigo_sdp` | Single line of text | |
| `tipo_cultivo` | Single line of text | |
| `variedad_fruta` | Single line of text | |
| `numero_cuartel` | Single line of text | Puede tener ceros a la izquierda o letras |
| `nombre_cuartel` | Single line of text | |
| `predio` | Single line of text | |
| `sector` | Single line of text | |
| `lote_productor` | Single line of text | Lote campo del proveedor |
| `color` | Single line of text | Escala interna del cliente |
| `estado_fisico` | Single line of text | Se registra en recepción |
| `a_o_r` | Choice | Valores: Aprobado / Objetado / Rechazado |
| `n_guia` | Single line of text | Puede ser alfanumérico |
| `transporte` | Single line of text | Tipo de transporte en que llegó el bin |
| `capataz` | Single line of text | |
| `codigo_contratista` | Single line of text | |
| `nombre_contratista` | Single line of text | |
| `hora_recepcion` | Single line of text | Formato `HH:mm` |
| `kilos_bruto_ingreso` | Decimal number | |
| `kilos_neto_ingreso` | Decimal number | Restricción: ≤ `kilos_bruto_ingreso` |
| `n_cajas_campo` | Whole number | |
| `observaciones` | Multiline text | |
| `rol` | Single line of text | Perfil de usuario que registra |
| `operator_code` | Single line of text | |
| `source_system` | Single line of text | |
| `source_event_id` | Single line of text | |
| `dataverse_id` | Single line of text | Nullable — reservado para sincronización futura |
| `created_at` | → columna nativa `Created On` | No crear manualmente |
| `updated_at` | → columna nativa `Modified On` | No crear manualmente |
 
---
 
## LotePlanta

Agrupación interna generada en planta. Código operacional: `LP-{temporada_codigo}-NNNNNN`.

**Restricciones de negocio:** `kilos_neto_conformacion` no puede superar `kilos_bruto_conformacion`. El campo `disponibilidad_camara_desverdizado` solo es relevante cuando `requiere_desverdizado = true`; en caso contrario queda en `no_aplica`.

| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `id_lote_planta` | Single line of text | PK interna generada por el sistema |
| `lote_code` | Single line of text | Código operacional visible. Formato `LP-{temporada_codigo}-NNNNNN`. Generado automáticamente. |
| `temporada_codigo` | Single line of text | Código de temporada operativa. Ej: `2025-2026`. Se persiste explícitamente. |
| `correlativo_temporada` | Whole number | Correlativo ascendente dentro de la temporada. No se reutiliza si el lote es anulado. |
| `estado` | Choice | `Abierto` / `En proceso` / `Cerrado` / `Finalizado` / `Anulado` |
| `fecha_conformacion` | Date only | |
| `cantidad_bins` | Whole number | Guardado para consulta rápida |
| `kilos_bruto_conformacion` | Decimal number | Nuevo pesaje — no suma de bins |
| `kilos_neto_conformacion` | Decimal number | Restricción: ≤ `kilos_bruto_conformacion` |
| `requiere_desverdizado` | Two options | Sí / No — determinado en terreno al conformar el lote |
| `disponibilidad_camara_desverdizado` | Choice | `Disponible` / `No disponible` / `No aplica`. Nullable cuando `requiere_desverdizado = No`. Puede actualizarse. |
| `rol` | Single line of text | Perfil de usuario que registra |
| `operator_code` | Single line of text | |
| `source_system` | Single line of text | |
| `source_event_id` | Single line of text | |
| `dataverse_id` | Single line of text | Nullable |
| `created_at` | → columna nativa `Created On` | No crear manualmente |
| `updated_at` | → columna nativa `Modified On` | No crear manualmente |

> **Nota sobre `estado`:** implementar como `Choice` en Dataverse con cinco valores. Las transiciones de estado las gestiona el backend Django; no agregar Business Rules en Dataverse que bloqueen el campo.
 
---
 
## BinLotePlanta
 
Tabla de relación entre Bin y LotePlanta. Un bin aparece en una sola fila.
 
| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `bin_id` | Lookup → Bin | Único — restricción de unicidad sobre este campo |
| `lote_planta_id` | Lookup → LotePlanta | Un lote puede tener múltiples bins |
 
---
 
## CamaraMantencion
 
Etapa condicional pre-desverdizado. Un lote tiene como máximo un registro. Solo se crea cuando `requiere_desverdizado = Sí` y `disponibilidad_camara_desverdizado = No`.
 
**Restricciones de negocio:** `hora_salida` no puede ser anterior a `hora_ingreso`.
 
| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `id_camara_mantencion` | Single line of text | PK — corresponde al número de la cámara |
| `lote_planta_id` | Lookup → LotePlanta | Único — un lote tiene un solo registro de mantención |
| `fecha_ingreso` | Date only | |
| `hora_ingreso` | Single line of text | Formato `HH:mm` |
| `fecha_salida` | Date only | Nullable — se completa cuando la cámara de desverdizado queda disponible |
| `hora_salida` | Single line of text | Nullable. Restricción: no puede ser anterior a ingreso |
| `temperatura_camara` | Decimal number | °C |
| `humedad_relativa` | Decimal number | % |
| `camara_numero` | Single line of text | |
| `observaciones` | Multiline text | |
| `rol` | Single line of text | Perfil de usuario que registra |
| `operator_code` | Single line of text | |
| `created_at` | → columna nativa `Created On` | No crear manualmente |
| `updated_at` | → columna nativa `Modified On` | No crear manualmente |
 
---
 
## Desverdizado
 
Etapa condicional. Un lote planta tiene como máximo un registro. Solo se crea cuando `requiere_desverdizado = Sí` y la cámara está disponible. Etapa **funcionalmente vigente** del contrato operativo.
 
**Restricciones de negocio:** `hora_salida` no puede ser anterior a `hora_ingreso`. `kilos_neto_salida` no puede superar `kilos_bruto_salida`.
 
| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `id_desverdizado` | Single line of text | PK — corresponde a la fecha + la PK del lote procesado |
| `lote_planta_id` | Lookup → LotePlanta | Único — un lote pasa una sola vez |
| `fecha_ingreso` | Date only | |
| `hora_ingreso` | Single line of text | Formato `HH:mm` |
| `fecha_salida` | Date only | Nullable hasta que salga |
| `hora_salida` | Single line of text | Nullable. Restricción: no puede ser anterior a ingreso |
| `kilos_enviados_terreno` | Decimal number | Kilos recepcionados en recepción — referencia de origen |
| `kilos_recepcionados` | Decimal number | Pesaje al ingresar al frigorífico |
| `kilos_procesados` | Decimal number | Pesaje al salir |
| `kilos_bruto_salida` | Decimal number | |
| `kilos_neto_salida` | Decimal number | Restricción: ≤ `kilos_bruto_salida` |
| `color_salida` | Single line of text | Por si difiere del color del bin |
| `proceso` | Single line of text | Método de conservación — texto libre por ahora |
| `fecha_proceso` | Date only | |
| `sector` | Single line of text | |
| `cuartel` | Single line of text | |
| `rol` | Single line of text | Perfil de usuario que registra |
| `operator_code` | Single line of text | |
| `created_at` | → columna nativa `Created On` | No crear manualmente |
| `updated_at` | → columna nativa `Modified On` | No crear manualmente |
 
> `horas`, `horas_extras` y `total_horas` no se almacenan — se calculan desde fecha/hora de entrada y salida.
 
---
 
## CalidadDesverdizado
 
Control de calidad post-desverdizado. Solo aplica a lotes que pasaron por `Desverdizado`. Campos propuestos — pendientes de validación con cliente.
 
| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `id_calidad_desverdizado` | Single line of text | PK — corresponde a la fecha + la PK del lote procesado |
| `lote_planta_id` | Lookup → LotePlanta | |
| `fecha` | Date only | |
| `hora` | Single line of text | Formato `HH:mm` |
| `temperatura_fruta` | Decimal number | °C |
| `color_evaluado` | Single line of text | |
| `estado_visual` | Single line of text | |
| `presencia_defectos` | Two options | Sí / No |
| `descripcion_defectos` | Multiline text | Nullable |
| `aprobado` | Two options | Sí / No — aprobado para ingreso a packing |
| `observaciones` | Multiline text | |
| `rol` | Single line of text | Perfil de usuario que registra |
| `operator_code` | Single line of text | |
| `created_at` | → columna nativa `Created On` | No crear manualmente |
| `updated_at` | → columna nativa `Modified On` | No crear manualmente |
 
---
 
## IngresoAPacking
 
Etapa obligatoria para todos los lotes. Marca el cierre del flujo pre-packing. Un lote tiene exactamente un registro.
 
**Restricciones de negocio:** `kilos_neto_ingreso_packing` no puede superar `kilos_bruto_ingreso_packing`.
 
| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `id_ingreso_packing` | Single line of text | PK — corresponde a la fecha + la PK del lote procesado |
| `lote_planta_id` | Lookup → LotePlanta | Único — restricción de unicidad sobre este campo |
| `fecha_ingreso` | Date only | |
| `hora_ingreso` | Single line of text | Formato `HH:mm` |
| `kilos_bruto_ingreso_packing` | Decimal number | Pesaje al ingresar al área de packing |
| `kilos_neto_ingreso_packing` | Decimal number | Restricción: ≤ `kilos_bruto_ingreso_packing` |
| `via_desverdizado` | Two options | Sí = llegó desde desverdizado / No = directo. Campo de trazabilidad. |
| `observaciones` | Multiline text | |
| `rol` | Single line of text | Perfil de usuario que registra |
| `operator_code` | Single line of text | |
| `created_at` | → columna nativa `Created On` | No crear manualmente |
| `updated_at` | → columna nativa `Modified On` | No crear manualmente |
 
---
 
## RegistroPacking
 
Registra qué se produjo. Múltiples filas por lote según combinación de categoría, calibre y línea.
 
**Restricciones de negocio:** `merma_seleccion_pct` debe estar en rango 0–100.
 
| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `id_registro_packing` | Single line of text | PK — corresponde a la fecha + la PK del lote procesado |
| `lote_planta_id` | Lookup → LotePlanta | |
| `fecha` | Date only | |
| `hora_inicio` | Single line of text | Formato `HH:mm` |
| `linea_proceso` | Single line of text | Pendiente aclarar con cliente |
| `categoria_calidad` | Single line of text | Texto libre por ahora |
| `calibre` | Single line of text | |
| `tipo_envase` | Single line of text | |
| `cantidad_cajas_producidas` | Whole number | |
| `peso_promedio_caja_kg` | Decimal number | |
| `merma_seleccion_pct` | Decimal number | Restricción: rango 0–100 |
| `rol` | Single line of text | Perfil de usuario que registra |
| `operator_code` | Single line of text | Responsable línea |
| `created_at` | → columna nativa `Created On` | No crear manualmente |
| `updated_at` | → columna nativa `Modified On` | No crear manualmente |
 
---
 
## ControlProcesoPacking
 
Registra cómo estaba configurada la línea. Múltiples registros por lote si los parámetros cambian en el turno.
 
**Restricciones de negocio:** `rendimiento_lote_pct`, `pct_calibre_export` y `pct_calibres_menores` deben estar en rango 0–100.
 
| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `id_control_proceso_packing` | Single line of text | PK — corresponde a la fecha + la PK del lote procesado |
| `lote_planta_id` | Lookup → LotePlanta | |
| `fecha` | Date only | |
| `hora` | Single line of text | Formato `HH:mm` |
| `n_bins_procesados` | Whole number | |
| `velocidad_volcador` | Decimal number | bins/h |
| `obs_volcador` | Single line of text | |
| `temp_agua_tina` | Decimal number | °C |
| `cloro_libre_ppm` | Decimal number | |
| `ph_agua` | Decimal number | Escala 0–14 |
| `tiempo_inmersion_seg` | Whole number | Segundos enteros |
| `recambio_agua` | Two options | Sí / No |
| `temp_aire_secado` | Decimal number | °C |
| `velocidad_ventiladores` | Decimal number | |
| `fruta_sale_seca` | Two options | Sí / No |
| `tipo_cera` | Single line of text | |
| `dosis_cera_ml_min` | Decimal number | |
| `temp_cera` | Decimal number | °C |
| `cobertura_uniforme` | Two options | Sí / No |
| `n_operarios_seleccion` | Whole number | |
| `fruta_dano_condicion_kg` | Decimal number | |
| `fruta_dano_calidad_kg` | Decimal number | |
| `fruta_pudricion_kg` | Decimal number | |
| `merma_total_seleccion_kg` | Decimal number | |
| `equipo_calibrador` | Single line of text | |
| `calibre_predominante` | Single line of text | |
| `pct_calibre_export` | Decimal number | % — restricción: rango 0–100 |
| `pct_calibres_menores` | Decimal number | % — restricción: rango 0–100 |
| `tipo_caja` | Single line of text | |
| `peso_promedio_caja_kg` | Decimal number | |
| `n_cajas_producidas` | Whole number | |
| `rendimiento_lote_pct` | Decimal number | % — restricción: rango 0–100 |
| `observaciones_generales` | Multiline text | |
| `rol` | Single line of text | Perfil de usuario que registra |
| `operator_code` | Single line of text | Responsable turno |
| `created_at` | → columna nativa `Created On` | No crear manualmente |
| `updated_at` | → columna nativa `Modified On` | No crear manualmente |
 
---
 
## Pallet
 
Unidad de agrupación final. Código de negocio: `YYYYMMDD-P0001`. Primera entidad de la secuencia lineal post-packing.
 
**Restricciones de negocio:** `id_pallet` es inmutable una vez emitido. `peso_total_kg` y `cajas_por_pallet` deben ser ≥ 0.
 
| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `id_pallet` | Single line of text | PK de negocio. Inmutable. |
| `contador_incremental` | Whole number | Resetea por día |
| `fecha` | Date only | Dispara la generación del código |
| `hora` | Single line of text | Formato `HH:mm` |
| `tipo_caja` | Single line of text | |
| `cajas_por_pallet` | Whole number | Restricción: ≥ 0 |
| `peso_total_kg` | Decimal number | Restricción: ≥ 0 |
| `destino_mercado` | Single line of text | |
| `rol` | Single line of text | Perfil de usuario que registra |
| `operator_code` | Single line of text | Responsable paletizado |
| `dataverse_id` | Single line of text | Nullable — reservado para sincronización futura |
| `created_at` | → columna nativa `Created On` | No crear manualmente |
| `updated_at` | → columna nativa `Modified On` | No crear manualmente |
 
---
 
## PalletLotePlanta
 
Tabla de relación entre Pallet y LotePlanta. Un lote aparece en una sola fila.
 
| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `pallet_id` | Lookup → Pallet | Un pallet puede tener múltiples lotes |
| `lote_planta_id` | Lookup → LotePlanta | Único — restricción de unicidad sobre este campo |
 
---
 
## CalidadPallet
 
Control de calidad post-paletizaje. Segunda etapa de la secuencia lineal post-packing. Campos propuestos — pendientes de validación con cliente.
 
| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `id_calidad_pallet` | Single line of text | PK — corresponde a la fecha + la PK del pallet procesado |
| `pallet_id` | Lookup → Pallet | Un pallet puede tener múltiples registros |
| `fecha` | Date only | |
| `hora` | Single line of text | Formato `HH:mm` |
| `temperatura_fruta` | Decimal number | °C |
| `peso_caja_muestra` | Decimal number | kg |
| `estado_embalaje` | Single line of text | |
| `estado_visual_fruta` | Single line of text | |
| `presencia_defectos` | Two options | Sí / No |
| `descripcion_defectos` | Multiline text | Nullable |
| `aprobado` | Two options | Sí / No — aprobado para ingreso a cámara de frío |
| `observaciones` | Multiline text | |
| `rol` | Single line of text | Perfil de usuario que registra |
| `operator_code` | Single line of text | |
| `created_at` | → columna nativa `Created On` | No crear manualmente |
| `updated_at` | → columna nativa `Modified On` | No crear manualmente |
 
---
 
## CamaraFrio
 
Registro de ingreso y salida del pallet en cámara de frío. Tercera etapa de la secuencia lineal post-packing. Múltiples pallets pueden ingresar a la misma cámara. Un pallet no cambia de cámara.
 
**Restricciones de negocio:** `hora_salida` no puede ser anterior a `hora_ingreso`.
 
| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `id_camara_frio` | Single line of text | PK — corresponde al número de cámara |
| `pallet_id` | Lookup → Pallet | Único — un pallet tiene un solo registro de cámara |
| `camara_numero` | Single line of text | Múltiples pallets pueden referenciar el mismo número |
| `temperatura_camara` | Decimal number | °C |
| `humedad_relativa` | Decimal number | % |
| `fecha_ingreso` | Date only | |
| `hora_ingreso` | Single line of text | Formato `HH:mm` |
| `fecha_salida` | Date only | Nullable — se completa al despachar |
| `hora_salida` | Single line of text | Nullable. Restricción: no puede ser anterior a ingreso |
| `destino_despacho` | Single line of text | |
| `rol` | Single line of text | Perfil de usuario que registra |
| `operator_code` | Single line of text | |
| `created_at` | → columna nativa `Created On` | No crear manualmente |
| `updated_at` | → columna nativa `Modified On` | No crear manualmente |
 
---
 
## MedicionTemperaturaSalida
 
Medición de temperatura del pallet al salir de la cámara de frío, previo al despacho. Última etapa de la secuencia lineal post-packing.
 
| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `id_medicion_temperatura_salida` | Single line of text | PK — corresponde a la fecha + la PK del pallet procesado |
| `pallet_id` | Lookup → Pallet | Un pallet puede tener múltiples mediciones |
| `fecha` | Date only | |
| `hora` | Single line of text | Formato `HH:mm` |
| `temperatura_pallet` | Decimal number | °C — temperatura superficial del pallet |
| `punto_medicion` | Single line of text | Descripción del punto donde se tomó la medición |
| `dentro_rango` | Two options | Sí / No — si está dentro del rango aceptable |
| `observaciones` | Multiline text | |
| `rol` | Single line of text | Perfil de usuario que registra |
| `operator_code` | Single line of text | |
| `created_at` | → columna nativa `Created On` | No crear manualmente |
| `updated_at` | → columna nativa `Modified On` | No crear manualmente |
 
---
 
## SequenceCounter (Correlativos)

Tabla auxiliar de backend para la generación automática de códigos operacionales. Gestiona los correlativos de Bin, LotePlanta y Pallet de forma atómica.

**Pendiente de implementación en Dataverse.** En el corte actual, esta tabla existe solo en SQLite. El nombre sugerido en Dataverse es `CaliPro_correlativos`.

| Campo | Tipo Dataverse | Notas |
|---|---|---|
| `CaliPro_entity_name` | Single line of text | `'lote'`, `'bin'`, `'pallet'` |
| `CaliPro_dimension` | Single line of text | Clave de agrupación: `temporada_codigo` para lote; `YYYYMMDD` para bin y pallet |
| `CaliPro_last_value` | Whole number | Último correlativo asignado. El siguiente es `last_value + 1` |
| `created_at` | → columna nativa `Created On` | No crear manualmente |
| `updated_at` | → columna nativa `Modified On` | No crear manualmente |

**Restricción:** `UNIQUE (CaliPro_entity_name, CaliPro_dimension)`.

> La lógica de incremento atómico en Dataverse requiere una estrategia de concurrencia explícita (optimistic concurrency con ETag o tabla de bloqueo). El backend Django es el único responsable de gestionar los correlativos — no deben usarse Power Automate ni fórmulas de Dataverse para esto, para mantener consistencia entre los modos SQLite y Dataverse.

---

## Consideraciones al crear las tablas en Dataverse
 
**Lookup y campo de visualización.** Dataverse usa GUIDs internamente. Al configurar cada Lookup hay que indicar el campo de visualización — en todos los casos debe ser el código de negocio (`id_bin`, `id_lote_planta`, `id_pallet`).
 
**Unicidad.** Las restricciones de unicidad de `id_bin`, `id_lote_planta` y `id_pallet` se implementan como **Alternate Key**. Para `BinLotePlanta.bin_id`, `PalletLotePlanta.lote_planta_id` e `IngresoAPacking.lote_planta_id`, la unicidad se valida en la capa de aplicación.
 
**Inmutabilidad de códigos.** La inmutabilidad de `id_bin` e `id_pallet` se implementa bloqueando el campo en los formularios una vez guardado, o mediante Power Automate.
 
**Restricciones de rango y consistencia temporal.** Se implementan como **Business Rules** en Dataverse para cada tabla.
 
**Campo `disponibilidad_camara_desverdizado`.** Este campo en `LotePlanta` es nullable en el modelo conceptual (no aplica cuando `requiere_desverdizado = false`). Dado que `Two options` en Dataverse no admite null, se recomienda implementarlo como `Choice` con tres valores: `Disponible`, `No disponible`, `No aplica`. Alternativamente, añadir una Business Rule que lo oculte o bloquee cuando `requiere_desverdizado = No`. La decisión queda a criterio del equipo de implementación.
 
**Campo `rol`.** `Single line of text` en todas las tablas de proceso. El backend validará los 5 perfiles permitidos y la lógica de acceso.
 
**Choice `a_o_r`.** Local Option Set con tres opciones: `Aprobado`, `Objetado`, `Rechazado`.
 
**Campos `Two options`.** Renombrar las etiquetas al nombre del campo para mayor claridad en los formularios.
 
**Deuda técnica pendiente.** Los campos `linea_proceso`, `categoria_calidad` y `proceso` (Desverdizado) son `Single line of text` por ahora. Cuando el cliente entregue los valores definidos, se migran a `Choice`. Los campos de `CalidadDesverdizado` y `CalidadPallet` están propuestos — deben validarse con el cliente.
 
**Nota sobre temporada.** No existe campo `temporada` en ninguna tabla. Las consultas y agrupaciones por temporada se construyen con filtros de rango de fechas sobre `fecha_cosecha` en `Bin`. Esta lógica corresponde al backend o a las vistas y reportes que se construyan sobre el modelo.
 
---
 
---

## Tablas nuevas — Planillas de Control de Calidad (2026-04-06)

Cuatro tablas nuevas para digitalizar las planillas Excel del módulo Control de Calidad (rol `Control`).

---

### `crf21_planilla_desv_calibre` — Planilla Calibres Desverdizado

Entity Set: `crf21_planilla_desv_calibres`
Lookup padre: `crf21_lote_planta_id` → `crf21_lote_plantas`

| Columna Dataverse | Tipo | Descripción |
|---|---|---|
| `crf21_planilla_desv_calibreid` | Unique identifier (PK) | GUID autogenerado |
| `crf21_lote_planta_id` | Lookup → `crf21_lote_plantas` | Lote asociado |
| `crf21_supervisor` | Single line of text | Nombre supervisor |
| `crf21_productor` | Single line of text | Nombre productor |
| `crf21_variedad` | Single line of text | Variedad |
| `crf21_trazabilidad` | Single line of text | Código trazabilidad |
| `crf21_cod_sdp` | Single line of text | Código SDP |
| `crf21_fecha_cosecha` | Date only | Fecha cosecha |
| `crf21_fecha_despacho` | Date only | Fecha despacho |
| `crf21_cuartel` | Single line of text | Cuartel |
| `crf21_sector` | Single line of text | Sector |
| `crf21_oleocelosis` | Whole number | Cantidad defecto oleocelosis |
| `crf21_heridas_abiertas` | Whole number | Cantidad heridas abiertas |
| `crf21_rugoso` | Whole number | Cantidad rugoso |
| `crf21_deforme` | Whole number | Cantidad deforme |
| `crf21_golpe_sol` | Whole number | Cantidad golpe de sol |
| `crf21_verdes` | Whole number | Cantidad verdes |
| `crf21_pre_calibre_defecto` | Whole number | Cantidad pre calibre (defecto) |
| `crf21_palo_largo` | Whole number | Cantidad palo largo |
| `crf21_calibres_grupos` | Multiline text | JSON: 3 grupos de color con calibres |
| `crf21_observaciones` | Multiline text | Observaciones generales |
| `crf21_rol` | Single line of text | Rol operador |
| `crf21_operator_code` | Single line of text | Código operador |

---

### `crf21_planilla_desv_semillas` — Planilla Semillas Desverdizado

Entity Set: `crf21_planilla_desv_semillas`
Lookup padre: `crf21_lote_planta_id` → `crf21_lote_plantas`

| Columna Dataverse | Tipo | Descripción |
|---|---|---|
| `crf21_planilla_desv_semillasid` | Unique identifier (PK) | GUID autogenerado |
| `crf21_lote_planta_id` | Lookup → `crf21_lote_plantas` | Lote asociado |
| `crf21_fecha` | Date only | Fecha de evaluación |
| `crf21_supervisor` | Single line of text | Nombre supervisor |
| `crf21_productor` | Single line of text | Nombre productor |
| `crf21_variedad` | Single line of text | Variedad |
| `crf21_cuartel` | Single line of text | Cuartel |
| `crf21_sector` | Single line of text | Sector |
| `crf21_trazabilidad` | Single line of text | Código trazabilidad |
| `crf21_cod_sdp` | Single line of text | Código SDP |
| `crf21_color` | Single line of text | Color evaluado |
| `crf21_frutas_data` | Multiline text | JSON: 50 entradas {n_fruto, n_semillas} |
| `crf21_total_frutos_muestra` | Whole number | Total frutos muestreados |
| `crf21_total_frutos_con_semillas` | Whole number | Total frutos con semillas |
| `crf21_total_semillas` | Whole number | Total semillas |
| `crf21_pct_frutos_con_semillas` | Decimal number | % frutos con semillas |
| `crf21_promedio_semillas` | Decimal number | Promedio semillas por fruto |
| `crf21_rol` | Single line of text | Rol operador |
| `crf21_operator_code` | Single line of text | Código operador |

---

### `crf21_planilla_calidad_packing` — Planilla Calidad Packing Cítricos

Entity Set: `crf21_planilla_calidad_packings`
Lookup padre: `crf21_pallet_id` → `crf21_pallets`

| Columna Dataverse | Tipo | Descripción |
|---|---|---|
| `crf21_planilla_calidad_packingid` | Unique identifier (PK) | GUID autogenerado |
| `crf21_pallet_id` | Lookup → `crf21_pallets` | Pallet asociado |
| `crf21_productor` | Single line of text | Productor |
| `crf21_trazabilidad` | Single line of text | Trazabilidad |
| `crf21_cod_sdp` | Single line of text | Código SDP |
| `crf21_cuartel` | Single line of text | Cuartel |
| `crf21_sector` | Single line of text | Sector |
| `crf21_nombre_control` | Single line of text | Nombre control |
| `crf21_n_cuadrilla` | Single line of text | N° cuadrilla |
| `crf21_supervisor` | Single line of text | Supervisor |
| `crf21_fecha_despacho` | Date only | Fecha despacho |
| `crf21_fecha_cosecha` | Date only | Fecha cosecha |
| `crf21_numero_hoja` | Whole number | Número de hoja (1 o 2) |
| `crf21_tipo_fruta` | Single line of text | clementina / mandarina |
| `crf21_variedad` | Single line of text | orogrande, oronules, clemenule, tango |
| `crf21_temperatura` | Decimal number | Temperatura (°C) |
| `crf21_humedad` | Decimal number | Humedad (%) |
| `crf21_horas_cosecha` | Single line of text | H/Cos |
| `crf21_color` | Single line of text | Color evaluado |
| `crf21_n_frutos_muestreados` | Whole number | N° frutos muestreados |
| `crf21_brix` | Decimal number | BRIX promedio |
| `crf21_pre_calibre` | Whole number | Pre calibre (cantidad) |
| `crf21_sobre_calibre` | Whole number | Sobre calibre (cantidad) |
| `crf21_color_contrario_evaluado` | Whole number | Color contrario evaluado |
| `crf21_cantidad_frutos` | Whole number | Cantidad frutos |
| `crf21_ausencia_roseta` | Whole number | Ausencia roseta |
| `crf21_deformes` | Whole number | Deformes |
| `crf21_frutos_con_semilla` | Whole number | Frutos con semilla |
| `crf21_n_semillas` | Whole number | N° semillas |
| `crf21_fumagina` | Whole number | Fumagina |
| `crf21_h_cicatrizadas` | Whole number | H. cicatrizadas |
| `crf21_manchas` | Whole number | Manchas |
| `crf21_peduculo_largo` | Whole number | Pedúnculo largo |
| `crf21_residuos` | Whole number | Residuos |
| `crf21_rugosos` | Whole number | Rugosos |
| `crf21_russet_leve_claros` | Whole number | Russet leve claros |
| `crf21_russet_moderados_claros` | Whole number | Russet moderados claros |
| `crf21_russet_severos_oscuros` | Whole number | Russet severos oscuros |
| `crf21_creasing_leve` | Whole number | Creasing leve |
| `crf21_creasing_mod_sev` | Whole number | Creasing moderado/severo |
| `crf21_dano_frio_granulados` | Whole number | Daño frío/granulados |
| `crf21_bufado` | Whole number | Bufado |
| `crf21_deshidratacion_roseta` | Whole number | Deshidratación roseta |
| `crf21_golpe_sol` | Whole number | Golpe de sol |
| `crf21_h_abiertas_superior` | Whole number | H. abiertas superior |
| `crf21_h_abiertas_inferior` | Whole number | H. abiertas inferior |
| `crf21_acostillado` | Whole number | Acostillado |
| `crf21_machucon` | Whole number | Machucón |
| `crf21_blandos` | Whole number | Blandos |
| `crf21_oleocelosis` | Whole number | Oleocelosis |
| `crf21_ombligo_rasgado` | Whole number | Ombligo rasgado |
| `crf21_colapso_corteza` | Whole number | Colapso corteza |
| `crf21_pudricion` | Whole number | Pudrición |
| `crf21_dano_arana_leve` | Whole number | Daño araña leve |
| `crf21_dano_arana_moderado` | Whole number | Daño araña moderado |
| `crf21_dano_arana_severo` | Whole number | Daño araña severo |
| `crf21_dano_mecanico` | Whole number | Daño mecánico |
| `crf21_otros_condicion` | Single line of text | Otros condición |
| `crf21_total_defectos_pct` | Decimal number | Total defectos % |
| `crf21_rol` | Single line of text | Rol operador |
| `crf21_operator_code` | Single line of text | Código operador |

---

### `crf21_planilla_calidad_camara` — Planilla Control Proceso Cámara

Entity Set: `crf21_planilla_calidad_camaras`
Lookup padre: `crf21_pallet_id` → `crf21_pallets` (opcional / nullable)

| Columna Dataverse | Tipo | Descripción |
|---|---|---|
| `crf21_planilla_calidad_camaraid` | Unique identifier (PK) | GUID autogenerado |
| `crf21_pallet_id` | Lookup → `crf21_pallets` (nullable) | Pallet asociado (opcional) |
| `crf21_fecha_control` | Date only | Fecha del control |
| `crf21_tipo_proceso` | Single line of text | Tipo de proceso |
| `crf21_zona_planta` | Single line of text | Zona/planta |
| `crf21_tunel_camara` | Single line of text | Túnel/cámara |
| `crf21_capacidad_maxima` | Single line of text | Capacidad máxima |
| `crf21_temperatura_equipos` | Single line of text | Temperatura de equipos |
| `crf21_codigo_envases` | Single line of text | Código de envases |
| `crf21_cantidad_pallets` | Whole number | Cantidad de pallets |
| `crf21_especie` | Single line of text | Especie |
| `crf21_variedad` | Single line of text | Variedad |
| `crf21_fecha_embalaje` | Date only | Fecha de embalaje |
| `crf21_estiba` | Single line of text | Estiba |
| `crf21_tipo_inversion` | Single line of text | Tipo de inversión |
| `crf21_mediciones` | Multiline text | JSON: filas horarias de temperatura |
| `crf21_temp_pulpa_ext_inicio` | Decimal number | Promedio pulpa ext. inicio |
| `crf21_temp_pulpa_ext_termino` | Decimal number | Promedio pulpa ext. término |
| `crf21_temp_pulpa_int_inicio` | Decimal number | Promedio pulpa int. inicio |
| `crf21_temp_pulpa_int_termino` | Decimal number | Promedio pulpa int. término |
| `crf21_temp_ambiente_inicio` | Decimal number | Promedio ambiente inicio |
| `crf21_temp_ambiente_termino` | Decimal number | Promedio ambiente término |
| `crf21_tiempo_carga_inicio` | Single line of text | Hora inicio carga |
| `crf21_tiempo_carga_termino` | Single line of text | Hora término carga |
| `crf21_tiempo_descarga_inicio` | Single line of text | Hora inicio descarga |
| `crf21_tiempo_descarga_termino` | Single line of text | Hora término descarga |
| `crf21_tiempo_enfriado_inicio` | Single line of text | Hora inicio enfriado |
| `crf21_tiempo_enfriado_termino` | Single line of text | Hora término enfriado |
| `crf21_observaciones` | Multiline text | Observaciones |
| `crf21_nombre_control` | Single line of text | Nombre control proceso |
| `crf21_rol` | Single line of text | Rol operador |
| `crf21_operator_code` | Single line of text | Código operador |

> **JSON fields:** `crf21_calibres_grupos`, `crf21_frutas_data` y `crf21_mediciones` se almacenan como Multiline Text con contenido JSON serializado. El backend serializa/deserializa con `json.dumps`/`json.loads`.

---

*Subpágina de [03 - Modelo de datos y entidades](03-Modelo-de-datos-y-entidades). Complementa [03.2 - Especificación del modelo](03.2-‐-Especificación-del-modelo) y [03.3 - Diagrama entidad-relación (ERD)](03.3-%E2%80%90-Diagrama-entidad-%E2%80%90-relaci%C3%B3n-(ERD)).*